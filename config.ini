; Configuration moved to config/config.ini
; Keep this small pointer for backward compatibility.
; Please edit config/config.ini instead of this file.

[meta]
note = "Primary configuration moved to config/config.ini"
